Flask>=3.0.0
Flask-CORS>=4.0.0
spotipy>=2.23.0
Pillow>=10.0.0
requests>=2.31.0
python-dateutil>=2.8.2
pytz>=2023.3
pyowm>=3.3.0
espn-api>=0.32.0

# Optional: mDNS advertising so the device can reach the companion at
# http://matrixportal-companion.local:5001 instead of a hardcoded IP.
zeroconf>=0.130.0
